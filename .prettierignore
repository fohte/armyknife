# Auto-generated files
CHANGELOG.md

# Copier rewraps this to a width prettier doesn't agree with, so the two
# fight over its formatting on every `copier update`.
.copier-answers.yml

# Generated by `cargo run -p config-schema-gen`; CI regenerates it, so
# reformatting here only creates a diff that gets reverted.
docs/config-schema.json
